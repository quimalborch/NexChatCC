# Vercel Postgres Database URLs
# Direct connection for migrations and CLI commands
POSTGRES_URL="postgres://your_full_postgres_url_here"

# Prisma Accelerate URL for application connections (faster)
PRISMA_DATABASE_URL="prisma+postgres://your_full_accelerate_url_here"

# Additional Vercel Postgres URLs (if provided)
POSTGRES_PRISMA_URL="your_prisma_url_if_available"
POSTGRES_URL_NON_POOLING="your_non_pooling_url_if_available"